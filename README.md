# News Analyzer Agent

The idea here is that given $n$ news items in a real-time pipeline,

Next Steps:
- Create an MCP tool for `/ticker` `/context`

potential ideas: import news PDFs into RAG database

create an .env with different api keys. available models



this is where you can add your API keys for the different models as well as add MCP servers

you can also generate your own `.env` and feed it in the class instantiation.

make sure to uncomment `fastagent.secret.yaml` in `.gitignore`